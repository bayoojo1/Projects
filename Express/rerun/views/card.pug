extends layout.pug

block content
  section#content 
    h2= text
  if side === "question"
    p
      i Hint: #{hint}
  a(href=`${id}?side=${sideToShow}`)= sideToShowDisplay
  br
  a(href='/cards') Next card